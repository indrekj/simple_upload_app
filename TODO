* Sweeper that removes unused categories
* WRITE SOME TESTS!!!
* Change the url when selecting a category (but no refresh)
* After user uploads an asset, show it in the assets table (if same category)
